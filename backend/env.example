# ===================================================================
# MINUS VOICE ASSISTANT - ENVIRONMENT CONFIGURATION
#
# Instructions:
# 1. Rename this file to .env
# 2. Fill in the required values for the services you want to use.
# 3. For services you aren't using, you can leave the values blank.
#    The application will run in "mock mode" for those services.
# ===================================================================

# -------------------------------------------------------------------
# LLM MODEL SELECTION (Choose Your AI Brain!)
# -------------------------------------------------------------------
# Choose your LLM model provider. Supported options:
# - "gemma":     Use Google's Gemma model via a Service Account JSON file. (Free, Recommended Start)
# - "lm_studio": Use a model running locally via LM Studio. (Free, Offline, Customizable)
# - "qwen":      Use a premium model like Qwen via OpenRouter. (Paid, High-Performance)
LLM_MODEL=lm_studio

# --- LM Studio Configuration ---
# Required if LLM_MODEL is "lm_studio"
LM_STUDIO_API_BASE="http://localhost:1234/v1"
LM_STUDIO_MODEL_NAME="local-model" # e.g., "gemma-2-9b-it-gguf"

# --- Gemma Configuration (Service Account) ---
# Required if LLM_MODEL is "gemma"
# Path to your Google Cloud service account JSON file.
GEMINI_CREDENTIALS_PATH=credentials/gemini_credentials.json

# --- Qwen / OpenRouter Configuration ---
# Required if LLM_MODEL is "qwen"
OPENROUTER_API_KEY=your_openrouter_api_key_here
# You can also use Groq for faster inference by uncommenting the next line
# GROQ_API_KEY=your_groq_api_key_here


# -------------------------------------------------------------------
# GOOGLE SERVICES OAUTH (For Gmail, Calendar, etc.)
# -------------------------------------------------------------------
# This single credentials file will be used for all Google services.
# Follow GMAIL_SETUP_GUIDE.md to create this file.
GOOGLE_OAUTH_CREDENTIALS_PATH=credentials/google_oauth_credentials.json

# Directory to store the authentication tokens generated by Google.
# The application needs write access to this directory.
GOOGLE_TOKEN_DIR=tokens/


# -------------------------------------------------------------------
# DATABASE & CORE SERVICES
# -------------------------------------------------------------------
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173


# -------------------------------------------------------------------
# DEVELOPMENT & DEBUGGING
# -------------------------------------------------------------------
# Set to "development" or "production"
ENVIRONMENT=development
# Set to "true" to enable debug logging and features
DEBUG_MODE=true
# Set to "true" to force mock mode for all services, even if keys are present
FORCE_MOCK_MODE=false


# -------------------------------------------------------------------
# VOICE SYSTEM CONFIGURATION
# -------------------------------------------------------------------
# You can use a comma-separated list for wake and stop words.
WAKE_WORDS=hey minus,minus
STOP_WORDS=stop,never mind
USE_LOCAL_WHISPER=true

# Voice Server Settings
VOICE_SERVER_PORT=8001
VOICE_TIMEOUT_SECONDS=3

# Audio Processing
AUDIO_SAMPLE_RATE=44100
AUDIO_CHANNELS=1
VOICE_ACTIVATION_THRESHOLD=0.5

# Logging Level
LOG_LEVEL=INFO

# ==============================================
# USAGE INSTRUCTIONS
# ==============================================

# 1. QUICK START (FREE):
#    - Set LLM_MODEL=gemma
#    - Get free Google AI API key
#    - Leave other keys empty for mock mode

# 2. ENHANCED MODE (Premium):
#    - Set LLM_MODEL=qwen  
#    - Get OpenRouter API key for Qwen3 32B
#    - Set up Gmail/Calendar OAuth for real data

# 3. PRODUCTION MODE:
#    - Set ENVIRONMENT=production
#    - Configure all real API keys
#    - Set DEBUG_MODE=false

# ==============================================
# COST ESTIMATION
# ==============================================

# Gemma 3n: FREE (up to 14,400 requests/day)
# Qwen3 32B: ~$0.10/M input, $0.30/M output
# Estimated monthly cost for Qwen3 32B: RM50-80

# ==============================================
# ADDITIONAL PLATFORMS (Future)
# ==============================================

# Telegram Bot API
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# WhatsApp Business API  
WHATSAPP_API_KEY=your_whatsapp_api_key_here

# ==============================================
# AI Services
# ==============================================

# NEW: Google AI API for Gemma 3n (FREE Tier)
GOOGLE_API_KEY=your_google_ai_api_key

# Google APIs (OAuth)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_PROJECT_ID=your_google_project_id

# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Application Settings
DEBUG=true

# Voice Settings
TTS_PROVIDER=elevenlabs
STT_PROVIDER=openai

# NEW: FastRTC Voice Pipeline Configuration
FASTRTC_HOST=localhost
FASTRTC_PORT=8001 